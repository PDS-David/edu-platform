# Copy this to .env.local for local development
# On Render, set VITE_API_URL in the dashboard under AISchoolonair-frontend → Environment

# Local development (ngrok or localhost)
VITE_API_URL=http://localhost:5000

# Production (set this in Render dashboard, not here)
# VITE_API_URL=https://www.aischoolonair.ng
