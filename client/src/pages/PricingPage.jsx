// client/src/pages/PricingPage.jsx
// MVP: Pricing is not active. This page is a placeholder.
// UpgradeWall is also disabled — no paywall during MVP.

export default function PricingPage() {
  return null; // Route exists but pricing is not live yet
}

// UpgradeWall — disabled for MVP. Components that import this get a no-op.
export function UpgradeWall({ onRevise }) {
  return null;
}
